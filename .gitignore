node_modules/
dist/
migrations/
vendor/
# Bundled PGN Mentor collections — fetched on demand via `npm run fetch:pgn-mentor`.
server/data/championPgn/*.pgn
# Persisted in-memory storage snapshot at repo root only (games / analyses / training).
# Use leading slash so `client/src/data/…` is not ignored (e.g. Chess Tube catalog).
/data/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/
.idea/
